# IKA-66 の残り1項目 — 幅48 のプールで学習した世代は、幅24 のプールの世代に勝つか

〔2026-09-22、worktree `ika-66-width48-pool`〕

## なぜこれだけが残っているか

課題文の3つのうち、1 と 3 は 9/20〜9/22 に済んでいる。**再導出しないこと**
（`GENERATIONS.md`「幅はどこで止まるか」「生成における幅の値段は…」）:

```
  1. 生成スループット   測定済み。壁時計 2.43倍 / 1手あたり 2.76倍
                        （見積もりの 1.7倍ではない。控え隠蔽の枝で測り直した数字）
  3. 幅 64 / 96         96 は測定済み。50.50% ±0.60 で買えない。飽和は 48 にある
  2. プールと世代       ← これが残り。本ファイルはこれだけを扱う
```

そして記録自身が、算術では決着しないと書いている:

```
  利得（出荷条件）     +3.12 ±0.72          ← 固い
  費用 2.43倍 = 1.28 倍化ぶん = -2.2 〜 -4.6  ← 帯が広い。2点から引いた帯で、
  差し引き             +0.9 〜 -1.5             2点目の区間はゼロを含む
```

**またぐ理由は利得ではなく換算率のほう。** だから換算率を使わずに、プールを実際に作って
世代を戦わせる。

## 設計

### 独立変数は壁時計。`--limit` 以外は1つも動かさない

2腕を**逐次**に回す（同時に回すと機械を分け合い、独立変数そのものが壊れる）。
腕ごと 120分。生成レートから局数を決め、**実測の壁時計を両方記録する**。
ずれたら、多いほうのプールを添字で切って合わせる（局は添字から種を取るので、
先頭 N 局は「N 局を注文していたら得られたプール」そのもので、これが正しい切り方）。

今日の機械で測り直したスループット（各300局、`data/ika66/probe-w*`）:

```
  幅24   300局 / 109秒   165局/分      A腕の定常は 170局/分（1,028→1,578局を193秒）
  幅48   300局 / 256秒    70局/分
         ------------------------------
         比 2.35倍（記録の 2.43倍。300局は起動ぶんが効くので少し縮む）
```

```
  共通  leaf value-gen11L.pt / book rizabanadohido-value-gen11L.jsonl.gz
        --hide-bench --rank-leaf --served --servers 2 --workers 24
        run seed 6601、添字は両腕とも 0 から  → 局 i は両プールで同じ6匹・同じ選出
  A腕   --limit 24    120分ぶん
  B腕   --limit 48    120分ぶん（A腕の約 1/2.43 の局数）
```

### 学習はプール単独

出荷手順（既存83,000局 + 新プール）だと、腕の差は総データで 0.14倍化 = +0.2〜0.5点まで
薄まり、12,000局の対戦の分解能 ±0.7 の下に沈む。**単独で学習させると腕の比が
そのまま 1.28倍化**になり、課題文の算術と同じ土俵に乗る。

⚠ **動作点が出荷版より弱い側にずれる**（学習曲線の急な側で測ることになる）ので、
倍化1つの価値は出荷時より**大きめ**に出る。つまりこの測り方は**幅48 に不利**な側に偏る。
符号が幅48 なら、出荷手順でも幅48 と読んでよい。逆は読めない。

### 3本目の腕をタダで足す —— 量と質を分ける

B腕の局数だけ A腕のプールを**先頭から切った** C腕を作る（生成は不要、符号化するだけ）。

```
  A  幅24・多い局数（等時間）
  B  幅48・少ない局数（等時間）
  C  幅24・B と同じ局数（等局数、時間は 1/2.43）

  B 対 A = 出荷判断そのもの（等時間）
  B 対 C = プールの「質」だけ（等局数）
  A 対 C = プールの「量」だけ（既知の学習曲線の、この動作点での再測定）
```

### モデルは2本アンサンブル

`generation_match` の `--value` の help —— 同じ設定の6シードが保留AUC 0.024 に散り、
設定差は 0.004。**単独2本はシード運を測る。3本アンサンブル2つが変化を測る。**
ここは各腕2シード（`--seed 0` と `--seed 1`）、それ以外の学習設定は既定のまま同一。

### 対戦

```
  幅は両腕 24（現行の出荷幅）。差は葉だけ
  book は1つを共有  → 同じ6匹・同じ選出・同じ種。対応が残る
  --hide-bench      → プールが隠蔽生成なので、判定も隠蔽で
  6,000局 × 2席 = 12,000局  → ±0.7 点。予測帯 +0.9〜-1.5 の幅 2.4 は分解できる
```

### 帰無対照と発火率

**勝率は発火率と並べないと読めない**（IKA-12 の4節）。キュー対戦は同じ局を腕を入れ替えて
2回打つので、恒等な腕どうしは1手も違わず対の区間は厳密に 0 になる。

```
  負の対照   A腕アンサンブル 対 それ自身、200局 → 50.00% ±0.00 / 発火 0% を確認してから本番
  本番の発火 tools/pair_divergence.py で「手が変わったペア」の割合を出す
```

## 走らせる順序

| # | やること | 道具 | 見込み |
|---|---|---|---|
| 0 | スループット確認（300局×2） | `generate_queue.py` | **済** |
| 1 | 負の対照 200局 | `match_queue.py` | **済 —— 50.00% ±0.00 / 発火 0.0%** |
| 2 | A腕 生成 19,800局 | `run-ika66-generate.sh` | 2.0h |
| 3 | B腕 生成 8,400局 | 同上（逐次） | 2.0h |
| 4 | 壁時計を突き合わせ、添字で切って等時間に揃える | `run-ika66-truncate.py` | 5分 |
| 5 | 符号化 A / B / C・学習 3腕 × 2シード | `run-ika66-train.sh` | 30分 |
| 6 | B 対 A 12,000局（+ 勝率・対の区間・発火率） | `run-ika66-match.sh armB armA 6000 b-vs-a` | 1.8h |
| 7 | （余力）B 対 C 12,000局 | `run-ika66-match.sh armB armC 6000 b-vs-c` | 1.8h |

### 負の対照（step 1、本番の前に取った）

```
  value-gen11Lx2 対 それ自身、幅24・葉順位・book 共有・隠蔽、200局
  対の区間  50.00% ±0.00%（独立扱いなら ±6.93%）   Elo -0.0 [-0.0, -0.0]
  発火率    手を変えたペア 0 / 100（0.0%）、100組すべて完全一致
  1手あたり 0.9584s / 0.9588s = 1.00倍
```

## 生成の結果 —— レート比 2.400倍。記録の 2.43倍と一致する

```
  A腕 幅24   19,800局 / 6,022秒（100.3分）   197.3局/分   遊んだ機械 0.1%、終了差 9秒
  B腕 幅48    8,400局 / 6,131秒（102.1分）    82.2局/分   遊んだ機械 0.1%、終了差 13秒
                                              ---------
                                              2.400倍
```

**これは4本目の独立な測定**で、記録の 2.43倍（600局×3往復）とほぼ一致する。試走の300局で
2.35倍と出たのは起動ぶんが効いたためで、本番の長さでは記録どおりに戻った。

### 等時間への補正

壁時計が 1.8% ずれたので、実測から切って揃えた:

```
  等時間 6,022秒
    armA  幅24  19,800局（全部）
    armB  幅48   8,400 × 6022/6131 = 8,250局
    armC  幅24  armA の先頭 8,250局

  局数比 19,800 / 8,250 = 2.400 = 1.263 倍化   ← 課題文の「1.28 倍化」と一致
```

切り出しは3本とも `[0, N)` の添字が1つも欠けていないことを確認済み。

### 符号化 —— 腕ごとに1種類しか混ざっていない

```
  armA  19,800局 / 266,700決定   budget {'24': 19800}   leaf value-gen11L のみ
  armB   8,250局 / 111,533決定   budget {'48':  8250}   leaf value-gen11L のみ
  armC   8,250局 / 111,033決定   budget {'24':  8250}   leaf value-gen11L のみ
```

⚠ **保留AUC は腕の比較に使えない**（腕ごとに違う局面分布で測っている）。学習ログの数字は
記録として残すが、判定は盤の上だけで行う。

## ⚠ 生成中に master が動いた —— ただし `sources` は動いていない

別セッションが IKA-57 をマージし、`df83b33` → `5d0bbe1` になった。生成と対戦で
`engine.commit` が食い違うので確認した:

```
  両プール  commit df83b33 / sources d3b946f6a2d545ca / node 36c5643f63ae6494   100%
  対戦      commit 5d0bbe1 / sources d3b946f6a2d545ca / node 36c5643f63ae6494
```

マージが触ったのは `tools/` `tests/` `docs/` `TODO.md` `ci.yml` だけで、
**`src/pokeuraou/*.py` と `rust/src/*.rs` は1バイトも動いていない。**
`sources` がそれを名指しで言う（`provenance.py` がこの取り違えのために持っているフィールド）。

**2腕は同一コードで生成され、打っているのも同じコード。** commit だけを引くと
木が変わったように読めるので、この run を引用するときは `sources` のほうを書くこと。

## 結果 —— B 対 A は **46.55% ±0.83**。幅48 のプールは等時間で負ける

`data/matches/ika66-b-vs-a`、12,000局、128.0分、遊んだ機械 0.1%:

```
  armB（幅48 8,250局）対 armA（幅24 19,800局）   両側 幅24・葉順位・book 共有・隠蔽
  席ごと  49.12% / 43.98%
  合計    5,586 / 12,000 = 46.55%  ±0.83（対）  Elo -24.0 [-29.8, -18.2]
  1手あたり  0.7286s / 0.7224s = 1.01倍   ← 対戦の費用は両腕同じ。差は葉だけ
```

**発火率 100.0%**（6,000組のうち 5,998組で手が変わった。完全一致は2組）。
最初の差は turn 1 が 5,529組。**勝率は発火率と並べて読めるほうの数字**で、
恒等な腕が返す綺麗な 50% ではない。

### 算術の予測より下に出た

```
  予測（課題文）  +3.12 - (2.2 〜 4.6) = +0.9 〜 -1.5
  実測            -3.45 ±0.83            ← 帯の下に外れる
```

⚠ **外れた向きは、あらかじめ宣言しておいた偏りの向きと同じ。** プール単独の学習は
出荷版より弱い動作点なので、倍化1つの価値が記録の帯（+1.7〜3.6）より大きく出る。
利得が丸ごと乗っていたと仮定して逆算すると **1倍化あたり +5.20 ポイント**で、
帯の上端 +3.6 を超えている。

### だから、この1本では出荷手順の判断にならない

事前に書いたとおり —— **「符号が幅48 なら出荷手順でも幅48 と読んでよく、逆は読めない」。**
符号は幅24 側に出たので、**読めない側**に落ちた。理由は2つあり、この run では分けられない:

```
  (a) 動作点が急なので、量の項（1.263倍化）が出荷時より重く効いた
  (b) 幅48 の局は「教材として」良くない —— 盤の +3.12 は打つ強さであって、
      教える質ではない。両者が同じものだという証拠はどこにもない
```

**C腕がこれを分ける。** armC は幅24・armB と同じ 8,250局（＝ armA の先頭 8,250局）:

```
  B 対 C  等局数 → 量の項が消える。残るのは「幅48 の局は良い教材か」だけ
  A 対 C  等幅   → 量の項だけ。この動作点での「1倍化あたり何点か」が直接出る
                   （課題文の「この帯をもう一度測る」が、まさにこれ）
```

**B 対 C が 50% 近辺なら (b) で決まり**、動作点をどう動かしても幅48 は買えない。
**B 対 C が明確に幅48 側なら (a) で、出荷手順（累積プール）での判断は開いたまま**になり、
A 対 C が出す局所の倍化レートで外挿することになる。

## B 対 C は **49.41% ±0.82** —— 幅48 の局は、教材としては幅24 の局と区別がつかない

`data/matches/ika66-b-vs-c`、12,000局、129.9分。**等局数**（8,250 対 8,250）なので、
量の項は構造的に消えている。残るのは「幅48 で打った局は良い教材か」だけ:

```
  armB（幅48 8,250局）対 armC（幅24 8,250局）
  席ごと  51.60% / 47.22%
  合計    5,929 / 12,000 = 49.41%  ±0.82（対）  Elo -4.1 [-9.8, +1.6]
  発火率  100.0%（6,000組すべてで手が変わった。完全一致 0組）
  1手あたり 0.7347s / 0.7367s = 1.00倍
```

**区間はゼロを含み、点推定はわずかに幅24 側。** 発火率が 100% なので、これは
「手を変えない設定が返す綺麗な 50%」ではなく、**変えた上で差が出ていない**ほう。

### これが IKA-66 の答えを決める

```
  盤の上で幅48 が幅24 に勝つ    +3.12 ±0.72   （記録、出荷条件12,000局）
  幅48 の局が教材として勝つ     -0.59 ±0.82   （これ）
```

**打つ強さは教える質ではない。** 2つが同じものだという前提は、どこにも証拠が無いまま
採算の式に入っていた。等局数で測ると利得は消える。

量の項が消えている以上、**この読みは動作点に依存しない。** プール単独で測ったことも、
累積プールで測ることも、ここには効かない —— 幅48 の局が教材として同じなら、
2.4倍の壁時計は何も買っていない。

### ⚠ 1つ前の推論を取り下げる

B 対 A の節で「逆算すると1倍化あたり +5.20 ポイントで帯の上端を超えている」と書いたが、
あれは**「+3.12 の利得が丸ごと教材に乗っている」という前提で割った数字**だった。
B 対 C がその前提を否定したので、**あの +5.20 は前提が偽になった問いへの答え**であり、
この動作点の倍化レートの推定としては使えない。倍化レートは A 対 C が直接測る。

### A 対 C の予測を、走らせる前に書いておく

加法性が成り立つなら、A 対 C は B 対 C から B 対 A を引いたぶんになる:

```
  予測  52.86% ±1.17   （+2.86 ポイント）
        1.263 倍化ぶんなので  1倍化あたり +2.26 ポイント
        記録の帯 +1.7 〜 +3.6 の中に入る
```

**三角形が閉じれば**、この動作点の倍化レートは記録の帯と整合し、
「プール単独は動作点が急すぎる」という私の事前の懸念のほうが要らなかったことになる。
**閉じなければ**、加法性が近似であることの2度目の記録になる（IKA-12 の三角形は
1.76 ±1.55 だけ閉じなかった）。

## A 対 C は **53.16% ±0.82** —— 三角形は閉じ、換算率は帯の中に戻った

`data/matches/ika66-a-vs-c`、12,000局、134.1分。**等幅**（両腕とも幅24）なので、
残るのは量の項だけ:

```
  armA（19,800局）対 armC（8,250局）
  席ごと  54.97% / 51.35%
  合計    6,379 / 12,000 = 53.16%  ±0.82（対）  Elo +22.0 [+16.3, +27.7]
  発火率  100.0%（6,000組中 5,999組で手が変わった）
```

### 予測は走らせる前に書いてあった

```
  予測  52.86% ±1.17      実測  53.16% ±0.82      差 +0.30
```

3本を突き合わせても閉じる:

```
  B 対 A  予測 46.25 ±1.16（B-C と A-C から）   実測 46.55 ±0.83
          差 +0.30 ±1.43  ← 閉じる
```

IKA-12 の三角形は 1.76 ±1.55 だけ閉じなかった。**今回は閉じた。** 違いは、3本とも
同じ日・同じプール・同じ相手で、腕が葉だけ違うこと。

### この動作点の学習曲線の傾き

```
  +3.16 ポイント / 1.263 倍化 = 1倍化あたり +2.50 ポイント
  記録の帯 +1.7 〜 +3.6 の、ほぼ真ん中
```

課題文が「またぐ理由は利得ではなく換算率のほう。2点から引いた帯で、2点目の区間は
ゼロを含んでいる」と書いていた換算率が、**1本の対戦から ±0.82 で出た。帯は正しかった。**

⚠ **走らせる前に書いた私の懸念のほうが不要だった。** 「プール単独は動作点が急なので
倍化1つが帯より重く出る」と宣言していたが、+2.50 は帯の中で重くなかった。
**B 対 A の −3.45 は動作点のせいではない。** だから読めない側に落ちたという心配も
要らなくなり、結論は出荷手順にもそのまま通る。

## 結論 —— 幅は上げない。ただし理由が変わった

```
  これまで  「幅48 は 2.03倍払って −1.3 ＝タダ同然だから半分でいい」
            ← 1,696局・±2.4 の失敗。12,000局で +3.42 に化けて、すでに否定済み
  これから  「幅48 の局は教材として同じだから、生成で 2.43倍を払う理由が無い」
```

**対局（人間と45秒）の +3.12 は生きている。** 決着していなかったのは生成だけ。

コードの変更は3か所の docstring だけ:

```
  tools/generate_queue.py  --limit の「IKA-66 が両方を握っている」を、結果に置き換え
  src/pokeuraou/search.py  幅の節に「打つ強さは教える質ではない」を追記
  src/pokeuraou/search.py  データの節の「ゼロと区別がつかない」を +2.50 ±(勝率±0.82) に
```

## 置き場所

プールは `data/ika66/` の下に**入れ子で**置く。`data/selfplay-*` の glob に拾われないため
—— 9/20 に「幅の違う2つのプールが `data/` に残ると、後でどちらかが学習に混ざる」ので
3,600局を消した、という記録がある。ここでは消さずに、拾われない場所に置く。
