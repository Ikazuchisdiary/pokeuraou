# 短期の強化 TODO

順序は **期待 Elo ÷ 機械時間**。数字は測定から取っており、**測定値**と**見積もり**を
区別して書く。測定の出典は `GENERATIONS.md`。

単価が変わったことが前提にある。生成 7,000戦が5時間から30分、対戦840戦が70分から13分、
学習は30秒。**「測ったが高すぎて使えない」という分類はもう無い。**

---

## 済み

### A1. エンジンのフィンガープリントを記録する ✅ `426e304`

`src/pokeuraou/*.py` と `rust/src/*.rs` の blake2b ＋ git commit ＋ dirty フラグを
全ゲームに焼く。**記録していない軸は、後から必ず1つの名前に潰れる。**

### A2. エンコードを差分化する ✅ `b0b4380`

ディレクトリごとに `-encoded.npz` を作り、連結する。再利用の判定は**ファイルのバイトサイズ**
（mtime ではない）。連結時に `game` 番号をずらすのが要点 — そのまま繋ぐと別世代の同じ番号が
1ゲーム扱いになり、検証分割の両側に同じゲームが乗る。

### B1. 旧データを捨てる判断 ✅ **捨てない**

`fresh`（gen-5,6,7）対 `all`（+ gen-2,3,4）で fresh 48.1% [44.7, 51.5]。捨てる側には
はかいこうせん未修正という根拠があったが、それでも勝てなかった。

### B2. TD ターゲット ✅ **効かない**（`3aed146` / `76a824e`）

| λ | 検証 AUC | 盤上 |
|---|---|---|
| 0.0 | 0.8766 | — |
| 0.3 | 0.8850 | +2.7 [−0.7, +6.1]（848戦） |
| 0.7 | **0.8887** | **+1.2 [−1.2, +3.6]（1,696戦）** |

**検証は単調に改善するのに盤上は動かない。** λ を上げるほど前世代の探索の模倣に寄り、
系統的な偏りごと受け継ぐ。得たのは施策ではなく**測り方の教訓**で、今後「検証値だけで
採用しそうになったとき」のブレーキとして使う。

### B3. レーティングの鎖を繋ぐ ✅ `value-all/w48/leaf` = 139.9 Elo で首位

`value-all.pt/w48/leaf` 対 `value-gen234.pt/w48`、840戦、**56.8% [53.4, 60.1]**。
**繋がっていないエージェントの誤差は ±265 になり、1戦入れるだけで ±41 に落ちる。**

### A3. ベンチの測り方 ✅ `1736262` / `45c120e`

- **ゲーム1本のコストは8倍ばらつく**（直列・アイドル機で 1.77〜13.75 s/game）。
  ターン数ではなく葉の数。**1シードのベンチは測定ではなく抽選。**
- **セル数を仕事量として数えてはいけない**（ワーカー間で ±5% に揃うが s/game は3倍違う）。
- **8ワーカーで 5.6倍は正常。** 機械は壊れていない。
- **device は状況依存。** GPU は軽いゲームで2倍、CPU は重いゲームで14%。既定は cuda。
- 生成の既定を `WORKERS=8 / DEVICE=cuda / RUST_NODE=1` に変更。

---

## 実行中

### B2 の対戦 〔848戦・約20分〕

検証（両方とも実際の勝敗に対して）:

| ターゲット | AUC | log loss |
|---|---|---|
| λ=0（従来） | 0.8766 | 0.4298 |
| **λ=0.3** | **0.8850** | **0.4159** |
| 生成側の探索 | 0.8816 | 0.4248 |

**生の評価が生成した探索を初めて上回った。** 検証が良い ≠ 盤上で強い、なので対戦で確認中。

---

## E1. 選出が一様なままだった 〔測定済み・+18.9 ポイント〕 ← **B5 の傾きとは独立の軸**

**全エージェントが両側とも選出を90通りから一様に引いていた。** `generation_match.py:216`、
`generate_parallel.sh` の `BOOK` は空が既定、`data/selfplay-gen7` の `selectionSource` は
全件 `uniform`。**レーティングの尺度全体がこの条件で測られている。**

394チーム分の解（`data/selection/rizabanadohido-value-gen2.jsonl.gz`）を集計すると:

    一様選出の EV 損失   平均 0.2284  中央値 0.2259（>0.10 が 100%、>0.20 が 82.7%）
    均衡値               平均 0.6312
    支持の大きさ         平均 1.09/90、純戦略 359チーム

**ただしこれは book のセル値＝`value-gen2` 自身の見積り。推定であって測定ではない。**
測ったのが `tools/book_check.py`（4腕、ゲーム単位で対、幅16、`value-gen2`）:

| 腕 | ゲーム | 勝率 | 95% Wilson |
|---|---|---|---|
| `uniform/uniform` | 280 | 52.5% | [46.7, 58.3] |
| **`book/uniform`** | 280 | **71.4%** | [65.9, 76.4] |
| `book/book` | 280 | 54.6% | [48.8, 60.4] |
| `gen/gen` | 280 | 55.4% | [49.5, 61.1] |

**助言の価値 = +18.9 ポイント [+10.9, +27.0] = +141.5 Elo [+78, +218]。**

**いまの尺度で `hp-share`(0) から首位 `value-all/w48/leaf`(139.9) までの全距離と同じ。**
探索と学習を7世代積んで稼いだぶんが、選出を一様から均衡に変えるだけで並ぶ。

推定 22.8 に対して実測 18.9。**差の約4ポイントが optimiser's curse** — 解くのは 8,100 個の
推定セルに対する argmax に近く、推定誤差に最も弱い操作。この目減りは想定内。

**`book/book` は LP の主張 63.2% を 8.5 ポイント下回った**（54.6%）。向きは「ソルバが
こちらに楽観的」で、**弱い相手で学習した価値関数が相手の最善応手を過小評価する**という
予測された方向。

**`gen/gen` = 55.4% が生成の実際の分布。** gen-7 の実測 side 0 勝率 54.0% と整合する。
生成のログを均衡値 63.2% と比べるのは別物の比較で、比べるべきはこちら。

### 但し書き

- 測定は**幅16・`value-gen2`**（book を解いたモデル）。現行は**幅48・`value-all`** で、
  **現行設定での大きさは未知**。book の助言は2世代前の意見。
- 尺度への含意: 全エージェントが**両側とも**一様なので agent 間の比較は歪まないが、
  **全員が EV 損失 0.23 を共通して背負った状態**で戦術の差を測っている。
  選出の誤差が戦術の差を覆い隠している可能性がある。

### 次にやること

1. **生成を `BOOK` 付きで回す。** ε/T は被覆表の **0.25 / 0.5**（T=0.05 は最薄の選出に
   7,000ゲーム換算で 0 ゲームしか割り当てず、次の解が外挿になる）
2. `generation_match.py` に `--selection-book` を足す。**探索用の ε は持ち込まない** —
   探索ノイズは生成のものであって評価のものではない。評価は book の純戦略で引く
3. book 選出の腕は**新しいエージェント**。鎖に繋ぐには既存の腕と1マッチ（B3 の前例:
   840戦で ±265 → ±41）
4. `selectionSource` が `book` として記録されることを確認
5. 効果が小さかった場合の次の問い: **現行モデル `value-all` で解き直したらどうか**
   （1チーム約52秒 × 394、`solve_book_parallel.sh` あり）

**検証 AUC / log loss で判断しない**（`76a824e`: TD ターゲットは検証を単調に改善して
何も勝たなかった）。判定は勝率だけ。

### E1 が開けた、測り直すべき結論

**「探索側の梃子は出尽くした」は、すべて一様選出の下で測られている。**

| 結論 | 測定条件 | 測り直す理由 |
|---|---|---|
| 深さ 1→2 は −1.2 | 一様選出 | 選出が均衡なら局面分布が変わる。均衡選出は**相性の良い4体**を出すので、そこで深さが効くかは別の問い |
| narrow の regret ほぼゼロ（93%） | 一様選出・gen-7 の局面 | 同上。均衡選出の局面は毛色が違う |
| 幅 24→48 は +5.5 | 一様選出 | 同上 |
| TD ターゲットは効かない | 一様選出のデータ | ラベルの質が選出に依存する |

**全部を測り直すのは高いので、順序を決める必要がある。** いま分かっているのは
「選出の誤差 0.23 が全員に共通して乗っていた」ことで、それが戦術の差を圧縮していたなら、
**選出を直した後の傾きは今日の測定より急になりうる**。

最初に測り直すべきは**深さ**だと思う。今日「タダなのに買えない」と出た唯一の梃子で、
かつ「均衡の台が小さいから refine するセルが少ない」という説明が**選出に依存しない**
とは言い切れないため。

### E2. 現行モデルで book を解き直す 〔394チーム × 約52秒〕

book は `value-gen2` の意見。現行は `value-all` で、レーティング上 **80 Elo 以上**離れて
いる（`value-gen2/w24` 65.8 対 `value-all/w48/leaf` 139.9）。

`book/book` が LP の主張 63.2% を **8.5 ポイント下回った**のは、`value-gen2` が自陣を
過大評価している向き。**現行モデルで解き直せば、助言そのものが良くなる可能性が高い。**

`solve_book_parallel.sh` がある。ただし E1 の +18.9 は `value-gen2` の book で出した
実測なので、**解き直しは「さらに上がるか」の問いであって、+18.9 の前提ではない。**

---

## 次

### D1. 量と質のトレード 〔同じ機械時間で2本・各1〜2時間〕

**「質が悪くても大量」は成り立つか。** 測定は両方向に出ている:

- 支持: B1（バグ入りエンジンの旧3世代を捨てても勝てない、48.1%）、学習曲線が未飽和
- 反対: gen-3 単独学習は前世代に負ける（−4.0）、`value-gen2/w16` は `hp-share/w24` と
  区別できない（7.7 ± 45.6）、**B2 で弱い教師の模倣が偏りごと受け継ぐことが出た**

現状の読みは **「大量の低品質は補助としては有効、代替としては有害」**。

**生成コストの 99% 以上は探索であってゲームではない**（1決定あたり葉 4,601〜19,333）。
つまり「軽量に生成する」は「弱いプレイヤーに打たせる」とほぼ同義で、局面だけでなく
**ラベル（勝敗）も劣化する**。

実験は1回で済む。**同じ機械時間で:**

| 腕 | 設定 | ゲーム数 |
|---|---|---|
| 現行 | 幅48・学習済み葉・葉順位 | 約7,000 |
| 軽量 | 幅16・`hp-share` 葉・順位なし | 約70,000 |

セル数が 1/9、torch 無しなので1桁以上安い。学習して対戦させる。
**安いのは安い側なので、測定自体も安い。**

### D2. 局面とラベルの強さを分離する 〔`selfplay.py` に1ノブ〕

D1 は「局面を作る強さ」と「ラベルを作る強さ」を混ぜている。**分けられる:**

    局面は安く作る   最初の k ターンは探索を薄くして多様な局面まで歩く
    ラベルは高く作る そこから先は現行の強さで決着まで打つ

ラベルの質は後半だけで決まるので、**前半を捨て値にしても壊れない**。むしろ弱い手で
歩いたほうが**局面の多様性は上がる**（強い者同士は同じ筋に収束する）。

いまの生成は前半も後半も同じ強さで打っており、この分離を試していない。
**「同じ局面をもっと見る」より「違う局面を見る」**という方向なので C1 と噛み合う。

新しい機構は要らない。`--shallow-turns k` を足すだけ。

### D5. なぜ深さが効かないのか 〔安い順に2つ〕

**「探索しても強くならない」は測った内容より強い要約。** 幅は効いた（+8.8、+5.5）。
効かなかったのは深さだけで、しかも「深さ2」は素朴な2手読みではない。

**仮説: 探索は「足りないものを足す」ときは効き、「既に知っていることを歪んだモデルで
再導出する」ときは効かない。**

幅が直していたのは**欠落**（最善手がメニューに無い、63% → 93%）— 離散的で、直せば
直ったぶんだけ返る。深さが直そうとしているのは**値の精度**だが、値はすでに実際の勝敗で
学習した評価関数が与えている。

**再導出の経路が汚れている、という説はほぼ潰れた。** `Budget.matrix()` は命中も追加効果も
状態異常判定も**すべて列挙している**。まもるの失敗は `_do_protect` が 1/3^n で成功枝と
失敗枝を両方作っており、正しい。つぶれているのは**急所だけ**で、測定済みの policy TV は
0.0005（コスト 2.68倍なので残す判断も測定に基づく）。

    accuracy        policy TV 0.0001   1.08倍   列挙する
    crit            policy TV 0.0005   2.68倍   つぶす  ← これだけ
    status checks   policy TV 0.0040   0.97倍   列挙する（つぶしても速くならない）
    secondary       policy TV 0.0083   1.20倍   列挙する

〔訂正の経緯: 一度「偶然を4つつぶしている」と書いたが、それは
`tools/budget_effect.py` の**フラグの説明文**（各フラグを off にすると何が起きるか）を
実際の設定と取り違えたもの。向きも逆で、status checks をつぶした場合の決定論的な読みは
「まもるは必ず失敗する」であって「必ず成功する」ではない。〕

**残る説明は1つだけ:** refine の2手目は `sub_limit` / `sub_branches` で**1手目より狭い**。
**精度の低い評価を深いところに置いている** — 評価が雑なら深いほど悪化しうるという探索
pathology の形。

検証（安い順）:

1. **2手目のメニューを広げる**（`sub_limit` を上げる）〔数十分〕 — 狭さが原因なら符号が変わる
2. 急所を列挙して深さ2を測り直す〔2.68倍〕 — policy TV 0.0005 なので見込みは薄い

**1 が外れたら、「深さは値を良くするが、値はもう十分良い」が残る。** その場合に効くのは
評価関数の側（データ量・相手の控えの不完全情報）で、MCTS も同じ理由で効かない。

### D3. 現行設定で narrow の regret を測り直す ✅ **既に解消済み**

いまある regret の数字は**すべて幅24・ダメージ順位**で測ったもの:

    最良応手がメニューにあった      76/120（63%）
    regret 中央値 / p90 / 最大      0.000 / 0.079 / 0.321
    落とした手を指すと              +14.3 ポイント [+7.1, +21.5]

そのあと幅を48にし（+5.5）、順位付けを葉に変えた（+2.9 / +19 Elo）。**現行設定で
測り直していない。** 「幅48では regret はほぼ消えている」は保持率からの推定。

**測定結果（`value-all`、gen-7 の局面120個、幅48・葉順位）:**

| | 幅24・ダメージ順位 | **幅48・葉順位** |
|---|---|---|
| 最良応手がメニューにあった | 76/120（63%） | **112/120（93%）** |
| regret p90 / 最大 | 0.079 / 0.321 | **0.000** / 0.124 |
| 0.10 超 | 7% | **1%** |

**+14.3 ポイントの伸びしろは、幅48と葉順位で既に回収されていた。** 方策ヘッドを
「より良い48手を選ぶため」に作る根拠は無い。作るなら**同じ質をより少ない候補で**出す
ためだが、その出口（深さ）が −1.2 なので、そちらも塞がっている（D5 を参照）。

**ただし regret は探索自身の葉に対して測っている。** 「メニューが葉の一番好きな手を
落とさなくなった」であって「客観的な最善手が入っている」ではない。**誤差はメニューから
葉へ移っただけ。**

### D4. narrow をペア単位からスロット単位へ 〔D3 の後〕

narrow が選んでいるのは**ペア**（スロット2つ × 各10手前後 = 合法ペア数百）。順位付けの
コストは**ペア数に比例**するので、幅を下げるほど順位付けが高くつく（幅24で +75%、幅48で
ほぼタダ）。

方策ヘッドを**スロット単位**で評価して上位の組み合わせを作れば、この比例がほどける —
数百のペアではなく20手前後を評価するだけになる。**「軽量で質のよい枝刈り」の本体はここ。**

カバレッジ規則（全スロット選択肢が最低1回は出る）との関係は設計時に決める。いまは固い
規則だが、方策が確率を与えるなら「確率の下限」に置き換えられる可能性がある。

### B4. gen-7 の不足 2,021戦を補充 〔約20分〕

壊れた2ワーカーを落としたぶん。

### B5. 探索予算対 Elo の曲線 〔測定中〕

深さ2 対 深さ1 を**幅48・同じモデル・同じ順位付け・1,696戦**で測定中。過去の測定は
幅24・840戦で +2.1 [−1.2, +5.5] と決着しなかった。

これは「深さ2は有意か」ではなく **「予算を10倍すると何 Elo か」の傾き**の1点目として取る:

| 予算 | 構成 | Elo |
|---|---|---|
| 1× | w24 d1 | 基準 |
| 1.9× | w48 d1 | +38（測定済み） |
| ~5× | w48 d2 | 測定中 |

**傾きが浅ければ MCTS も方策ヘッドも +200 には届かず、構造（相手の控えの不完全情報）に
行くしかない。深ければ探索への投資が本線。** どちらでも次が一意に決まる。

観測: 深さ2 が深さ1 とほぼ同じ速度で回っている。**幅48では均衡の台が小さく、refine
すべきセルが少ない**ということ。幅24で 2.72倍だったコストが幅48ではほとんど乗らない。
**幅を広げると深さが安くなる。**

---

## C. 汎用化に向けて（構造的な変更）

### C1. side 0 のチームを可変にする 〔生成器の変更〕 ← **汎用化の本丸**

いまは **side 0 が常に固定の6体**。汎用化に必要なのは同じ局面をもっと見ることではなく、
**違うチームを見ること**。side 0 も大会プールから引くだけで、同じゲーム数が桁違いに
広いチーム空間を覆う。

副作用として **R（構築の優位）が消える**。いま全対戦で厄介者になっている席の項が
不要になり、測定が綺麗になる。

### C2. 規模の計算 〔参考〕

1ゲーム 13.2決定・129 KB。**「局面数」は samples ではない** — 全決定が同じラベルを
共有するので、独立な情報はゲーム数。

| 目標 | ゲーム数 | 現行速度 | JSONL |
|---|---|---|---|
| 100万局面 | 約76,000 | 約10時間 | 約10 GB |
| 1,000万局面 | 約758,000 | 約4.2日 | 約98 GB |

学習曲線はまだ飽和していない（倍にするたび log loss が 0.02〜0.05 落ちる）ので
量は効く。ただし B2 を先に測ってから。

---

## やらないこと（測定済み）

- **幅48 より広げる** — 幅48で既に合法43ペア中34を保持。ただし **regret を測ったわけでは
  なく、保持率からの推定**（下の D3 を参照）。
- **候補順位を外す** — 幅48ではほぼタダ（0.245 → 0.249 s/turn = +1.6%）、幅24では75%増。
  **幅を下げるときほど順位付けは効く。**

  一度「`narrow` がプロセスの 37.7% を占めるので、葉順位を外せば sparse の取り分が戻る
  のでは」という疑いが出たが、**コードで決着した**: `_bridged_scores`（ダメージ採点）は
  `--rank-leaf` の有無に関係なく毎回走り、被覆規則と同点処理にも使われる。葉順位はその上に
  乗る増分で、+1.6% と矛盾しない。**37.7% は葉順位の費用ではない。**

  幅48での葉順位の **Elo は未測定**（+2.9 / +19 Elo は幅24の測定で、幅48では「ほぼタダ
  だから入れた」）。根拠としては弱いが、**費用が +1.6% なら外して得られるのも +1.6%**
  なので、優先度は低い。

## 速度担当に渡した材料（こちらは測らない）

境界を指す測定として4本挙げたが、**1本は誤り、1本は誤帰属**だった。残ったのは2本:

| 主張 | 判定 |
|---|---|
| python 635 CPU秒 対 Rust ノード 124 CPU秒 | **残る。** 中身は narrow 37.7% + torch 34.2% + LP 6.6% — **実作業であって JSON ではない** |
| GPU 使用率 76% で 51.6 W | **残る。** `from_encoded` は cuda で 8行でも 2,048行でも 2.1 ms。**全部が固定費** |
| 幅48のノードは数十MBの JSON、1往復が重い | **誤り。** 局面の JSON は 0.1%、1往復の固定費は 1.6〜3.9 ms |
| sparse がセル80%減で速度12%増 | **誤帰属。** `narrow` が横で一定額を払っているから |

3本目は `_read_exactly` のスタックと「64 MB」という数字からの**推測**で、位相別に
測っていなかった。**バイト数の大きさをコストの大きさと読み替えた。**

現在の的は「**フラグに関係なく毎決定で払っているダメージ採点（37.7%）**」で、既に Rust に
移植済みなので呼び出し回数と往復の話。バッチをまとめれば torch の 87%（プロセスの約30%、
1.4倍）が取れる、という上限も出ている。

## 保留（前提が崩れた）

- **Python の最適化** — 「Rust 84% / Python 4% だから触らない」が前提だった。これは
  `batched_payoffs` の**壁時間シェア**で、CPU秒で割ると **python 635秒 対 Rust ノード
  124秒**と逆転する（同じ785秒の窓、cuda・稼働中の実測）。

  ただし**この内訳は device を変えると動く**。速度担当の測定では torch が1プロセスに
  占める割合は cpu で 35.4%、cuda で 9.4%（幅48）。既定を cuda にしたので、python 側に
  残っているのは torch ではなく**橋渡し**（幅48のノードは数十MBの JSON）の疑いが濃い。

  **機構は未確定なので、まだ何もしない。** 次に測るのは境界のコストそのもの —
  `rustnode._exchange` を「リクエスト生成 / ノードの往復 / 応答の展開」に分けて計時する。
  これは最適化ではなく計測なので、既存の判断を壊さない。

- **「GPU を増やす」の旧根拠は無効** — 「forward pass は10%しかない」は Python 解決器時代の
  数字だった。移植後の幅48では cpu で 35.4%。既定が cuda になったので、この項目自体が
  「やらないこと」から外れる。
