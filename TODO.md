# 短期の強化 TODO

順序は **期待 Elo ÷ 機械時間**。数字は測定から取っており、**測定値**と**見積もり**を
区別して書く。測定の出典は `GENERATIONS.md`。

単価が変わったことが前提にある。生成 7,000戦が5時間から30分、対戦840戦が70分から13分、
学習は30秒。**「測ったが高すぎて使えない」という分類はもう無い。**

---

## 済み

### A1. エンジンのフィンガープリントを記録する ✅ `426e304`

`src/pokeuraou/*.py` と `rust/src/*.rs` の blake2b ＋ git commit ＋ dirty フラグを
全ゲームに焼く。**記録していない軸は、後から必ず1つの名前に潰れる。**

### A2. エンコードを差分化する ✅ `b0b4380`

ディレクトリごとに `-encoded.npz` を作り、連結する。再利用の判定は**ファイルのバイトサイズ**
（mtime ではない）。連結時に `game` 番号をずらすのが要点 — そのまま繋ぐと別世代の同じ番号が
1ゲーム扱いになり、検証分割の両側に同じゲームが乗る。

### B1. 旧データを捨てる判断 ✅ **捨てない**

`fresh`（gen-5,6,7）対 `all`（+ gen-2,3,4）で fresh 48.1% [44.7, 51.5]。捨てる側には
はかいこうせん未修正という根拠があったが、それでも勝てなかった。

### B2. TD ターゲット（実装のみ）✅ `3aed146`

`--td-lambda` で `(1-L)*outcome + L*searchValue` を学習。検証は常に実際の勝敗。
**まだ測っていない。** 再エンコード（`objectives` を記録するため）が要る。

### B3. レーティングの鎖を繋ぐ ✅ `value-all/w48/leaf` = 139.9 Elo で首位

`value-all.pt/w48/leaf` 対 `value-gen234.pt/w48`、840戦、**56.8% [53.4, 60.1]**。
**繋がっていないエージェントの誤差は ±265 になり、1戦入れるだけで ±41 に落ちる。**

### A3. ベンチの測り方 ✅ `1736262` / `45c120e`

- **ゲーム1本のコストは8倍ばらつく**（直列・アイドル機で 1.77〜13.75 s/game）。
  ターン数ではなく葉の数。**1シードのベンチは測定ではなく抽選。**
- **セル数を仕事量として数えてはいけない**（ワーカー間で ±5% に揃うが s/game は3倍違う）。
- **8ワーカーで 5.6倍は正常。** 機械は壊れていない。
- **device は状況依存。** GPU は軽いゲームで2倍、CPU は重いゲームで14%。既定は cuda。
- 生成の既定を `WORKERS=8 / DEVICE=cuda / RUST_NODE=1` に変更。

---

## 実行中

### B2 の対戦 〔848戦・約20分〕

検証（両方とも実際の勝敗に対して）:

| ターゲット | AUC | log loss |
|---|---|---|
| λ=0（従来） | 0.8766 | 0.4298 |
| **λ=0.3** | **0.8850** | **0.4159** |
| 生成側の探索 | 0.8816 | 0.4248 |

**生の評価が生成した探索を初めて上回った。** 検証が良い ≠ 盤上で強い、なので対戦で確認中。

---

## 次（安い順）

### B4. gen-7 の不足 2,021戦を補充 〔約20分〕

壊れた2ワーカーを落としたぶん。

### B5. 深さ2 を測り直す 〔約40分〕

レーティングでは +13 Elo（91.8 対 78.6）だが、ペアワイズでは +2.1 [−1.2, +5.5] で
ゼロを含んでいた。コストは 2.72倍。3,000戦打てば決着する。

---

## C. 汎用化に向けて（構造的な変更）

### C1. side 0 のチームを可変にする 〔生成器の変更〕 ← **汎用化の本丸**

いまは **side 0 が常に固定の6体**。汎用化に必要なのは同じ局面をもっと見ることではなく、
**違うチームを見ること**。side 0 も大会プールから引くだけで、同じゲーム数が桁違いに
広いチーム空間を覆う。

副作用として **R（構築の優位）が消える**。いま全対戦で厄介者になっている席の項が
不要になり、測定が綺麗になる。

### C2. 規模の計算 〔参考〕

1ゲーム 13.2決定・129 KB。**「局面数」は samples ではない** — 全決定が同じラベルを
共有するので、独立な情報はゲーム数。

| 目標 | ゲーム数 | 現行速度 | JSONL |
|---|---|---|---|
| 100万局面 | 約76,000 | 約10時間 | 約10 GB |
| 1,000万局面 | 約758,000 | 約4.2日 | 約98 GB |

学習曲線はまだ飽和していない（倍にするたび log loss が 0.02〜0.05 落ちる）ので
量は効く。ただし B2 を先に測ってから。

---

## やらないこと（測定済み）

- **幅48 より広げる** — 幅48で既に合法43ペア中34を保持。regret はほぼ消えている。
- **候補順位を外す** — 幅48ではほぼタダ（0.245 → 0.249 s/turn）、幅24では75%増。
  **幅を下げるときほど順位付けは効く。**

## 保留（前提が崩れた）

- **Python の最適化** — 「Rust 84% / Python 4% だから触らない」が前提だった。これは
  `batched_payoffs` の**壁時間シェア**で、CPU秒で割ると **python 635秒 対 Rust ノード
  124秒**と逆転する（同じ785秒の窓、cuda・稼働中の実測）。

  ただし**この内訳は device を変えると動く**。速度担当の測定では torch が1プロセスに
  占める割合は cpu で 35.4%、cuda で 9.4%（幅48）。既定を cuda にしたので、python 側に
  残っているのは torch ではなく**橋渡し**（幅48のノードは数十MBの JSON）の疑いが濃い。

  **機構は未確定なので、まだ何もしない。** 次に測るのは境界のコストそのもの —
  `rustnode._exchange` を「リクエスト生成 / ノードの往復 / 応答の展開」に分けて計時する。
  これは最適化ではなく計測なので、既存の判断を壊さない。

- **「GPU を増やす」の旧根拠は無効** — 「forward pass は10%しかない」は Python 解決器時代の
  数字だった。移植後の幅48では cpu で 35.4%。既定が cuda になったので、この項目自体が
  「やらないこと」から外れる。
