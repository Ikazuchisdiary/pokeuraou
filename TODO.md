# 短期の強化 TODO

順序は **期待 Elo ÷ 機械時間**。数字は今日の測定から取っており、**測定値**と**見積もり**を
区別して書く。測定の出典は `GENERATIONS.md`。

単価が変わったことが前提にある。生成 7,000戦が5時間から30分、対戦840戦が70分から20分、
学習は30秒。**「測ったが高すぎて使えない」という分類はもう無い。**

---

## A. 機械を使わずに今できる（計算資源待ちの間に）

### A1. エンジンのフィンガープリントを記録する 〔数分・測定不能だが損失回避〕

自己対戦の記録は `searchObjective` `searchLimit` しか持たず、**エンジンの版が無い**。

今日これで3世代が汚染された（はかいこうせんの反動なしが gen-2〜4 に焼き付いている）。
同じ穴を今日2回踏んでいる — レーティングの初回当てはめでも、`rankings`/`depths` を
記録していなかったせいでエージェント3種が1つの名前に潰れた。

**記録していない軸は、後から必ず1つの名前に潰れる。**

1,000万局面規模でこれが起きると、どのデータがどの修正より前かを特定できない。
`resolve.py` + `moves.rs` などのハッシュを1フィールド足すだけ。

### A2. エンコードを差分化する 〔1時間・生成のたびに75分を節約〕

いまは毎回プール全体を再エンコードしている（27,613戦で156秒）。80万戦なら毎回75分。
世代ごとに `.npz` を作って連結する形に変える。

### A3. `bench_workers.py` の測り方を決めておく 〔設計のみ〕

短いベンチは起動コストと外乱に弱すぎて、今日1回壊れた表を出した（8→61.9、12→22.7、
16→25.2、20→79.2 games/min、隣接設定で6倍）。**実運用と同じ形**（launcher で1,000戦規模）
を2〜3水準、第1波は捨てる。これが唯一信用できる。

---

## B. 機械が空いたら、安い順に

### B1. 旧データを捨てる判断の検証 〔約1時間〕 ← **指示済みの本題**

新プール（gen-6 7,008・幅24 + gen-7 11,983・幅48 = 18,991戦、全部エンジン修正後・
葉順位付き）だけで学習したものと、新規＋旧（+20,613）で学習したものを対戦させる。

過去の測定は「**置き換えは誤り、足せ**」（置換 −4.0、累積 +3.6）。ただしそれは**質が
同程度の世代同士**の話で、今回は捨てる側に根拠がある。根拠があることと勝つことは別。

学習30秒 ×2、対戦20分。

### B2. TD ターゲット 〔学習30秒 + 対戦20分〕 ← **一番効きそうな未検証**

同じ検証データで、**生成側の探索の値（AUC 0.8868 / log loss 0.4170）が、学習した生の
評価（0.8824 / 0.4216）より一貫して良い**。そしてそれは `searchValue` として全決定に
記録済み。

いま学習しているのは試合の最終結果で、**ターン1では実質コインフリップに近い分散**を持つ。
1ゲーム13.2決定が全部同じラベルを共有しているので、独立な情報はゲーム数しかない。
探索値を混ぜれば、その構造そのものを緩められる。

量を増やす前にこれを測る。

### B3. レーティングの鎖を切る 〔20分 × 2〕

誤差が ±33〜47 と大きいのは、グラフが鎖状だから。`value-gen2/w16` は `value-gen2/w24`
を経由してしか他と繋がっていない。**離れたペアを直接戦わせる**のが一番効く:

- `value-gen2.pt/w16` 対 `value-gen2345.pt/w24`
- `hp-share/w24` 対 `value-gen234.pt/w48`（現行最強と 0 点の直接対決）

### B4. gen-7 の不足 2,021戦を補充 〔約20分〕

壊れた2ワーカーを落としたぶん。新バイナリ（葉の共有で1.65倍）で埋める。

### B5. 深さ2 を測り直す 〔約40分〕

レーティングでは +13 Elo（91.7 対 78.6）だが、ペアワイズでは +2.1 [−1.2, +5.5] で
ゼロを含んでいた。コストは 2.72倍。**当時は840戦が70分だったので諦めた。いまは20分。**
3,000戦打てば決着する。

---

## C. 汎用化に向けて（構造的な変更）

### C1. side 0 のチームを可変にする 〔生成器の変更〕 ← **汎用化の本丸**

いまは **side 0 が常に固定の6体**。汎用化に必要なのは同じ局面をもっと見ることではなく、
**違うチームを見ること**。side 0 も大会プールから引くだけで、同じゲーム数が桁違いに
広いチーム空間を覆う。

副作用として **R（構築の優位）が消える**。いま全対戦で厄介者になっている席の項が
不要になり、測定が綺麗になる。

### C2. 規模の計算 〔参考〕

1ゲーム 13.2決定・129 KB。**「局面数」は samples ではない** — 全決定が同じラベルを
共有するので、独立な情報はゲーム数。

| 目標 | ゲーム数 | 現行速度 | JSONL |
|---|---|---|---|
| 100万局面 | 約76,000 | 約10時間 | 約10 GB |
| 1,000万局面 | 約758,000 | 約4.2日 | 約98 GB |

学習曲線はまだ飽和していない（倍にするたび log loss が 0.02〜0.05 落ちる）ので
量は効く。ただし B2 を先に測ってから。

---

## やらないこと（測定済み）

- **Python の最適化** — 内訳は Rust 84% / torch 10% / LP 1.6% / その他 Python 約4%。
  Python を全部ゼロにしても15%。
- **GPU を増やす** — forward pass は10%しかない。要るのはコア数か Rust 解決器。
- **幅48 より広げる** — 幅48で既に合法43ペア中34を保持。regret はほぼ消えている。
- **候補順位を外す** — 幅48ではほぼタダ（0.245 → 0.249 s/turn）、幅24では75%増。
  **幅を下げるときほど順位付けは効く。**
