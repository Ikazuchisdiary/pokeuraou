# 短期の強化 TODO

順序は **期待 Elo ÷ 機械時間**。数字は測定から取っており、**測定値**と**見積もり**を
区別して書く。測定の出典は `GENERATIONS.md`。

単価が変わったことが前提にある。生成 7,000戦が5時間から30分、対戦840戦が70分から13分、
学習は30秒。**「測ったが高すぎて使えない」という分類はもう無い。**

---

## 済み

### A1. エンジンのフィンガープリントを記録する ✅ `426e304`

`src/pokeuraou/*.py` と `rust/src/*.rs` の blake2b ＋ git commit ＋ dirty フラグを
全ゲームに焼く。**記録していない軸は、後から必ず1つの名前に潰れる。**

### A2. エンコードを差分化する ✅ `b0b4380`

ディレクトリごとに `-encoded.npz` を作り、連結する。再利用の判定は**ファイルのバイトサイズ**
（mtime ではない）。連結時に `game` 番号をずらすのが要点 — そのまま繋ぐと別世代の同じ番号が
1ゲーム扱いになり、検証分割の両側に同じゲームが乗る。

### B1. 旧データを捨てる判断 ✅ **捨てない**

`fresh`（gen-5,6,7）対 `all`（+ gen-2,3,4）で fresh 48.1% [44.7, 51.5]。捨てる側には
はかいこうせん未修正という根拠があったが、それでも勝てなかった。

### B2. TD ターゲット ✅ **効かない**（`3aed146` / `76a824e`）

| λ | 検証 AUC | 盤上 |
|---|---|---|
| 0.0 | 0.8766 | — |
| 0.3 | 0.8850 | +2.7 [−0.7, +6.1]（848戦） |
| 0.7 | **0.8887** | **+1.2 [−1.2, +3.6]（1,696戦）** |

**検証は単調に改善するのに盤上は動かない。** λ を上げるほど前世代の探索の模倣に寄り、
系統的な偏りごと受け継ぐ。得たのは施策ではなく**測り方の教訓**で、今後「検証値だけで
採用しそうになったとき」のブレーキとして使う。

### B3. レーティングの鎖を繋ぐ ✅ `value-all/w48/leaf` = 139.9 Elo で首位

`value-all.pt/w48/leaf` 対 `value-gen234.pt/w48`、840戦、**56.8% [53.4, 60.1]**。
**繋がっていないエージェントの誤差は ±265 になり、1戦入れるだけで ±41 に落ちる。**

### A3. ベンチの測り方 ✅ `1736262` / `45c120e`

- **ゲーム1本のコストは8倍ばらつく**（直列・アイドル機で 1.77〜13.75 s/game）。
  ターン数ではなく葉の数。**1シードのベンチは測定ではなく抽選。**
- **セル数を仕事量として数えてはいけない**（ワーカー間で ±5% に揃うが s/game は3倍違う）。
- **8ワーカーで 5.6倍は正常。** 機械は壊れていない。
- **device は状況依存。** GPU は軽いゲームで2倍、CPU は重いゲームで14%。既定は cuda。
- 生成の既定を `WORKERS=8 / DEVICE=cuda / RUST_NODE=1` に変更。

---

## 実行中

### B2 の対戦 〔848戦・約20分〕

検証（両方とも実際の勝敗に対して）:

| ターゲット | AUC | log loss |
|---|---|---|
| λ=0（従来） | 0.8766 | 0.4298 |
| **λ=0.3** | **0.8850** | **0.4159** |
| 生成側の探索 | 0.8816 | 0.4248 |

**生の評価が生成した探索を初めて上回った。** 検証が良い ≠ 盤上で強い、なので対戦で確認中。

---

## 次

### D1. 量と質のトレード 〔同じ機械時間で2本・各1〜2時間〕

**「質が悪くても大量」は成り立つか。** 測定は両方向に出ている:

- 支持: B1（バグ入りエンジンの旧3世代を捨てても勝てない、48.1%）、学習曲線が未飽和
- 反対: gen-3 単独学習は前世代に負ける（−4.0）、`value-gen2/w16` は `hp-share/w24` と
  区別できない（7.7 ± 45.6）、**B2 で弱い教師の模倣が偏りごと受け継ぐことが出た**

現状の読みは **「大量の低品質は補助としては有効、代替としては有害」**。

**生成コストの 99% 以上は探索であってゲームではない**（1決定あたり葉 4,601〜19,333）。
つまり「軽量に生成する」は「弱いプレイヤーに打たせる」とほぼ同義で、局面だけでなく
**ラベル（勝敗）も劣化する**。

実験は1回で済む。**同じ機械時間で:**

| 腕 | 設定 | ゲーム数 |
|---|---|---|
| 現行 | 幅48・学習済み葉・葉順位 | 約7,000 |
| 軽量 | 幅16・`hp-share` 葉・順位なし | 約70,000 |

セル数が 1/9、torch 無しなので1桁以上安い。学習して対戦させる。
**安いのは安い側なので、測定自体も安い。**

### D2. 局面とラベルの強さを分離する 〔`selfplay.py` に1ノブ〕

D1 は「局面を作る強さ」と「ラベルを作る強さ」を混ぜている。**分けられる:**

    局面は安く作る   最初の k ターンは探索を薄くして多様な局面まで歩く
    ラベルは高く作る そこから先は現行の強さで決着まで打つ

ラベルの質は後半だけで決まるので、**前半を捨て値にしても壊れない**。むしろ弱い手で
歩いたほうが**局面の多様性は上がる**（強い者同士は同じ筋に収束する）。

いまの生成は前半も後半も同じ強さで打っており、この分離を試していない。
**「同じ局面をもっと見る」より「違う局面を見る」**という方向なので C1 と噛み合う。

新しい機構は要らない。`--shallow-turns k` を足すだけ。

### D3. 現行設定で narrow の regret を測り直す 〔数分〕 ← **方策ヘッドの設計がこれで決まる**

いまある regret の数字は**すべて幅24・ダメージ順位**で測ったもの:

    最良応手がメニューにあった      76/120（63%）
    regret 中央値 / p90 / 最大      0.000 / 0.079 / 0.321
    落とした手を指すと              +14.3 ポイント [+7.1, +21.5]

そのあと幅を48にし（+5.5）、順位付けを葉に変えた（+2.9 / +19 Elo）。**現行設定で
測り直していない。** 「幅48では regret はほぼ消えている」は保持率からの推定。

**測らずに方策ヘッドを作ってはいけない。** 結果で作る理由が変わり、理由が変われば
設計も変わる:

| 結果 | 方策ヘッドの価値 | 学習させるもの |
|---|---|---|
| regret がまだ大きい | **精度** — より良い48手を選ぶ | 葉の順位との一致 |
| ほぼ消えている | **安さ** — 同じ質をより少ない候補で | 少数候補での均衡の再現 |

後者でも作る価値はある。**候補数が減れば深さの単価が下がる**（B5 の観測: 幅48では
均衡の台が小さく、深さ2がほとんどタダ）。

    uv run --group learn python tools/narrow_regret.py         --dir data/selfplay-gen7 --limit 48 --value data/models/value-all.pt

### D4. narrow をペア単位からスロット単位へ 〔D3 の後〕

narrow が選んでいるのは**ペア**（スロット2つ × 各10手前後 = 合法ペア数百）。順位付けの
コストは**ペア数に比例**するので、幅を下げるほど順位付けが高くつく（幅24で +75%、幅48で
ほぼタダ）。

方策ヘッドを**スロット単位**で評価して上位の組み合わせを作れば、この比例がほどける —
数百のペアではなく20手前後を評価するだけになる。**「軽量で質のよい枝刈り」の本体はここ。**

カバレッジ規則（全スロット選択肢が最低1回は出る）との関係は設計時に決める。いまは固い
規則だが、方策が確率を与えるなら「確率の下限」に置き換えられる可能性がある。

### B4. gen-7 の不足 2,021戦を補充 〔約20分〕

壊れた2ワーカーを落としたぶん。

### B5. 探索予算対 Elo の曲線 〔測定中〕

深さ2 対 深さ1 を**幅48・同じモデル・同じ順位付け・1,696戦**で測定中。過去の測定は
幅24・840戦で +2.1 [−1.2, +5.5] と決着しなかった。

これは「深さ2は有意か」ではなく **「予算を10倍すると何 Elo か」の傾き**の1点目として取る:

| 予算 | 構成 | Elo |
|---|---|---|
| 1× | w24 d1 | 基準 |
| 1.9× | w48 d1 | +38（測定済み） |
| ~5× | w48 d2 | 測定中 |

**傾きが浅ければ MCTS も方策ヘッドも +200 には届かず、構造（相手の控えの不完全情報）に
行くしかない。深ければ探索への投資が本線。** どちらでも次が一意に決まる。

観測: 深さ2 が深さ1 とほぼ同じ速度で回っている。**幅48では均衡の台が小さく、refine
すべきセルが少ない**ということ。幅24で 2.72倍だったコストが幅48ではほとんど乗らない。
**幅を広げると深さが安くなる。**

---

## C. 汎用化に向けて（構造的な変更）

### C1. side 0 のチームを可変にする 〔生成器の変更〕 ← **汎用化の本丸**

いまは **side 0 が常に固定の6体**。汎用化に必要なのは同じ局面をもっと見ることではなく、
**違うチームを見ること**。side 0 も大会プールから引くだけで、同じゲーム数が桁違いに
広いチーム空間を覆う。

副作用として **R（構築の優位）が消える**。いま全対戦で厄介者になっている席の項が
不要になり、測定が綺麗になる。

### C2. 規模の計算 〔参考〕

1ゲーム 13.2決定・129 KB。**「局面数」は samples ではない** — 全決定が同じラベルを
共有するので、独立な情報はゲーム数。

| 目標 | ゲーム数 | 現行速度 | JSONL |
|---|---|---|---|
| 100万局面 | 約76,000 | 約10時間 | 約10 GB |
| 1,000万局面 | 約758,000 | 約4.2日 | 約98 GB |

学習曲線はまだ飽和していない（倍にするたび log loss が 0.02〜0.05 落ちる）ので
量は効く。ただし B2 を先に測ってから。

---

## やらないこと（測定済み）

- **幅48 より広げる** — 幅48で既に合法43ペア中34を保持。ただし **regret を測ったわけでは
  なく、保持率からの推定**（下の D3 を参照）。
- **候補順位を外す** — 幅48ではほぼタダ（0.245 → 0.249 s/turn）、幅24では75%増。
  **幅を下げるときほど順位付けは効く。**

## 保留（前提が崩れた）

- **Python の最適化** — 「Rust 84% / Python 4% だから触らない」が前提だった。これは
  `batched_payoffs` の**壁時間シェア**で、CPU秒で割ると **python 635秒 対 Rust ノード
  124秒**と逆転する（同じ785秒の窓、cuda・稼働中の実測）。

  ただし**この内訳は device を変えると動く**。速度担当の測定では torch が1プロセスに
  占める割合は cpu で 35.4%、cuda で 9.4%（幅48）。既定を cuda にしたので、python 側に
  残っているのは torch ではなく**橋渡し**（幅48のノードは数十MBの JSON）の疑いが濃い。

  **機構は未確定なので、まだ何もしない。** 次に測るのは境界のコストそのもの —
  `rustnode._exchange` を「リクエスト生成 / ノードの往復 / 応答の展開」に分けて計時する。
  これは最適化ではなく計測なので、既存の判断を壊さない。

- **「GPU を増やす」の旧根拠は無効** — 「forward pass は10%しかない」は Python 解決器時代の
  数字だった。移植後の幅48では cpu で 35.4%。既定が cuda になったので、この項目自体が
  「やらないこと」から外れる。
